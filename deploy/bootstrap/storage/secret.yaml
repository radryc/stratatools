apiVersion: v1
kind: Secret
metadata:
  name: monofs-secrets
  namespace: ${STORAGE_NAMESPACE}
type: Opaque
data:
  monofs-token: ${MONOFS_TOKEN_B64}
  monofs-encryption-key: ${MONOFS_ENCRYPTION_KEY_B64}
  minio-access-key: ${MINIO_ACCESS_KEY_B64}
  minio-secret-key: ${MINIO_SECRET_KEY_B64}
