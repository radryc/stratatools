apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: search-index-data
  namespace: ${STORAGE_NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${SEARCH_PVC_SIZE}
