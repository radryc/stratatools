apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: node-${SUFFIX}-data
  namespace: ${STORAGE_NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${NODE_PVC_SIZE}
