apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fetcher-${SUFFIX}-cache
  namespace: ${STORAGE_NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${FETCHER_PVC_SIZE}
