apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: ${STORAGE_NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${MINIO_PVC_SIZE}
