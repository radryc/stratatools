apiVersion: v1
kind: Service
metadata:
  name: guardian-ui
  namespace: ${GUARDIAN_NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: guardiand
  ports:
    - name: http
      port: ${GUARDIAN_UI_PORT}
      targetPort: ${GUARDIAN_UI_PORT}
