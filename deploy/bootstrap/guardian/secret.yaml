apiVersion: v1
kind: Secret
metadata:
  name: guardian-secrets
  namespace: ${GUARDIAN_NAMESPACE}
type: Opaque
data:
  monofs-token: ${MONOFS_TOKEN_B64}
  client-discovery-token: ${CLIENT_DISCOVERY_TOKEN_B64}
