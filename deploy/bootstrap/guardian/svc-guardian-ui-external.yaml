apiVersion: v1
kind: Service
metadata:
  name: guardian-ui-external
  namespace: ${GUARDIAN_NAMESPACE}
spec:
  type: ${EXTERNAL_SERVICE_TYPE}${EXTERNAL_SERVICE_SPEC}
  selector:
    app: guardiand
  ports:
    - name: http
      port: ${GUARDIAN_UI_PORT}
      targetPort: ${GUARDIAN_UI_PORT}
