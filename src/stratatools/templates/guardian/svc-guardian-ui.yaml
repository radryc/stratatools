apiVersion: v1
kind: Service
metadata:
  name: guardian-ui
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: guardiand
  ports:
    - name: http
      port: 8080
      targetPort: 8080
