apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: node-${SUFFIX}-data
  namespace: ${NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${NODE_PVC_SIZE}
