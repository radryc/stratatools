apiVersion: v1
kind: Service
metadata:
  name: search-index
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: monofs-search
  ports:
    - name: grpc
      port: 9100
      targetPort: 9100
