apiVersion: v1
kind: Secret
metadata:
  name: monofs-secrets
  namespace: ${NAMESPACE}
type: Opaque
data:
  monofs-token: ${MONOFS_TOKEN}
  monofs-encryption-key: ${MONOFS_ENCRYPTION_KEY}
  minio-access-key: ${MINIO_ACCESS_KEY}
  minio-secret-key: ${MINIO_SECRET_KEY}
